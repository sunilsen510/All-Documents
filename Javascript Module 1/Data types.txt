Data Types explain in hindi with examples and why when and where and how to use it 


बहुत बढ़िया 👏
अब हम step-by-step सीखेंगे —

🧠 JavaScript Data Types (डेटा टाइप्स)

in Hindi with examples + why, when, where, how to use

📘 1. Data Type क्या होता है?

👉 Data Type का मतलब है —
किसी variable में कौन-सी type की value store की गई है।

हर value का एक “type” होता है — जैसे number, text, true/false, object, list आदि।

मतलब:

Data Type यह बताता है कि “value किस प्रकार की है और उस पर कौन-से operations किए जा सकते हैं।”

💡 2. क्यों ज़रूरी है (Why use Data Types)?

क्योंकि JavaScript को यह जानना जरूरी है कि value के साथ कैसे behave करना है:

| Reason               | Example               |
| ------------------   | --------------------- |
| ✅ Calculation करना  | Numbers चाहिए         |
| ✅ Text दिखाना          | String चाहिए          |
| ✅ Decision लेना        | Boolean चाहिए         |
| ✅ Data Store करना    | Object या Array चाहिए |


📍 3. कहाँ इस्तेमाल करते हैं (Where)?

जब आप values store करते हो variables में

जब functions, APIs, या user input handle करते हो

जब आपको decide करना होता है कि कौन-सा data कैसे behave करेगा

⚙️ 4. JavaScript में दो Category होती हैं:

| Category                            | Types                                                    |
| ----------------------------------- | -------------------------------------------------------- |
| **Primitive Types**                 | String, Number, Boolean, Null, Undefined, Symbol, BigInt |
| **Non-Primitive (Reference Types)** | Object, Array, Function                                  |


🧩 5. Primitive Data Types (Basics)
🟢 (A) String (Text)

Text या characters store करने के लिए।

let name = "Sunil";
let city = 'Delhi';
let message = `Hello ${name}`;


✅ String हमेशा quotes में होती है ' ', " ", या ` `

📘 Use: जब आपको text दिखाना हो, messages या user names handle करने हों।

🔵 (B) Number

Numeric values (integer या decimal) store करने के लिए।

let age = 25;
let price = 99.99;
let temperature = -5;


✅ Numbers पर आप +, -, *, / जैसे operations कर सकते हो।

📘 Use: जब calculation, math, या counting करनी हो।

🟡 (C) Boolean

True या False value रखता है (logical decision के लिए)।

let isLoggedIn = true;
let isPaid = false;


📘 Use: जब आपको condition check करनी हो
(जैसे login status, switch ON/OFF, etc.)

⚪ (D) Undefined

जब variable declare किया गया हो लेकिन value assign नहीं की गई।

let data;
console.log(data); // undefined


📘 Use: जब value अभी आने वाली हो या API से load होनी हो।

⚫ (E) Null

जब आप जानबूझकर variable को empty रखना चाहो।

let user = null; // अभी कोई user नहीं है


📘 Use: जब value future में assign होनी है।

🟣 (F) Symbol (ES6)

Unique identity बनाने के लिए (Advanced use)

let id = Symbol("userID");


📘 Use: जब आपको unique key चाहिए object में (mostly frameworks में)

🔴 (G) BigInt (ES2020)

बहुत बड़े numbers store करने के लिए (जो normal Number से बाहर हों)

let bigNumber = 123456789012345678901234567890n;


📘 Use: जब बहुत बड़े integers handle करने हों (like crypto, finance apps)

🧱 6. Non-Primitive Data Types
🔶 (A) Object

Key-value pairs में data store करने के लिए।

let user = {
  name: "Ravi",
  age: 22,
  city: "Mumbai"
};


📘 Use: जब एक ही entity से जुड़ी multiple information रखनी हो।

🔷 (B) Array

Multiple values एक list में store करने के लिए।

let fruits = ["Apple", "Banana", "Mango"];


📘 Use: जब similar values की list हो (items, IDs, etc.)

🟩 (C) Function

Reusable code block जो कुछ काम करता है।

function greet() {
  console.log("Hello World!");
}
greet();


📘 Use: जब कोई काम बार-बार करना हो (reusable logic)

🧮 7. typeof Operator

किसी value का data type check करने के लिए:

let name = "Sunil";
console.log(typeof name); // string

let age = 25;
console.log(typeof age);  // number

let isAdmin = true;
console.log(typeof isAdmin); // boolean

🧠 8. Example — All Data Types in One Code
const name = "Sunil";       // string
const age = 24;             // number
const isStudent = true;     // boolean
let address;                // undefined
let score = null;           // null
const user = { id: 1, name: "Ravi" };  // object
const colors = ["red", "blue", "green"]; // array

🧩 9. Practical Real-Life Example
const user = {
  name: "Rakesh",
  age: 25,
  isAdmin: true,
  hobbies: ["Reading", "Coding", "Cricket"],
  address: { city: "Delhi", pin: 110001 }
};

console.log(`User: ${user.name}, City: ${user.address.city}`);


🟢 Output:

User: Rakesh, City: Delhi

🧾 10. Summary Table

| Type      | Example         | When to Use            |
| --------- | --------------- | ---------------------- |
| String    | `"Sunil"`       | Text या message के लिए |
| Number    | `10`, `45.5`    | Calculation के लिए     |
| Boolean   | `true`, `false` | Condition के लिए       |
| Undefined | `let x;`        | Value अभी नहीं मिली    |
| Null      | `let x = null;` | Empty intentionally    |
| Object    | `{name:"Ravi"}` | Structured data        |
| Array     | `[1,2,3]`       | List data              |
| Function  | `function(){}`  | Reusable logic         |
| BigInt    | `123456789n`    | Large numbers          |
| Symbol    | `Symbol("id")`  | Unique key             |

🧠 11. कैसे Use करें (How to Use)

पहले decide करो कि value text है या number या logical flag।

उसी के हिसाब से data type चुनो।

हमेशा const या let से declare करो।

Debug करने के लिए typeof से type check करो।

🧭 12. Example: When to Use Which

| Situation            | Data Type | Example                           |
| -------------------- | --------- | --------------------------------- |
| User Name            | String    | `"Sunil"`                         |
| Age                  | Number    | `25`                              |
| Login Status         | Boolean   | `true`                            |
| Cart Items           | Array     | `["Shoes", "Watch"]`              |
| User Details         | Object    | `{name: "Sunil", city: "Bhopal"}` |
| API Response Pending | Null      | `null`                            |


अगर तुम चाहो तो अगला topic मैं तुम्हें समझाऊँ 👇
💥 “Type Conversion & Type Casting” —
यानि string → number, number → boolean practically कैसे बदलते हैं
और कहाँ ज़रूरी होता है?

क्या बताऊँ वो अगला?


बहुत बढ़िया 👏 चलिए अब सीखते हैं —

🧠 JavaScript Type Conversion & Type Casting

(इन हिन्दी में समझो – क्यों, कब, कहाँ, और कैसे use करते हैं)

🔹 1. Type Conversion / Type Casting क्या है?

👉 Type Conversion (Type Casting) का मतलब है —
किसी value के data type को एक type से दूसरे type में बदलना।

उदाहरण के लिए:

"25" (string) ➝ 25 (number)

1 ➝ true

false ➝ "false"

🔸 2. Type Conversion के दो प्रकार होते हैं

| प्रकार                  | मतलब                                                 |
| ----------------------- | ---------------------------------------------------- |
| **Implicit Conversion** | जब JavaScript खुद type बदल देती है (auto conversion) |
| **Explicit Conversion** | जब आप खुद type बदलते हो (manual conversion)          |

🧩 3. Implicit Conversion (Automatic)

JavaScript जब expression में अलग-अलग type मिलते हैं,
तो खुद ही type बदल देती है।

🔹 Example 1:
let result = "5" + 10;
console.log(result); // "510"


➡️ JS ने number 10 को string "10" में बदल दिया।
क्योंकि “+” operator string के साथ concatenate करता है।

🔹 Example 2:
let result = "5" - 2;
console.log(result); // 3


➡️ JS ने "5" को number में बदलकर calculation किया।

🔹 Example 3:
console.log("10" * "2"); // 20
console.log("10" / "2"); // 5


➡️ Multiplication और division में हमेशा number conversion होता है।

🧠 4. Explicit Conversion (Manual Conversion)

आप खुद type बदलते हो —
इसके लिए built-in functions होते हैं:

| Convert To | Method      |
| ---------- | ----------- |
| Number     | `Number()`  |
| String     | `String()`  |
| Boolean    | `Boolean()` |

🔹 (A) String → Number
let num = Number("123");
console.log(num); // 123
console.log(typeof num); // number


📘 Use: जब user input string में आता है, और आपको calculation करनी है।

👉 Example:

let a = prompt("Enter first number:");
let b = prompt("Enter second number:");
let sum = Number(a) + Number(b);
console.log(sum);

🔹 (B) Number → String
let str = String(123);
console.log(str); // "123"
console.log(typeof str); // string


📘 Use: जब आपको text में दिखाना हो (UI या message में)।

🔹 (C) Boolean → Number
console.log(Number(true));  // 1
console.log(Number(false)); // 0


📘 Use: जब true/false को binary या logic में use करना हो।

🔹 (D) Number → Boolean
console.log(Boolean(1)); // true
console.log(Boolean(0)); // false


📘 Use: जब number को condition में check करना हो।

🔹 (E) String → Boolean
console.log(Boolean("Hello")); // true
console.log(Boolean(""));      // false


📘 Rule:

Non-empty string → true

Empty string → false

⚙️ 5. Truthy और Falsy Values

JavaScript में हर value Boolean में बदल सकती है।
कुछ values Falsy होती हैं (जो false मानी जाती हैं):

| Falsy Values        |
| ------------------- |
| `0`                 |
| `""` (empty string) |
| `null`              |
| `undefined`         |
| `NaN`               |
| `false`             |

बाकी सब values Truthy होती हैं (true मानी जाती हैं)।

🔹 Example:
if ("Hello") {
  console.log("This is True");
}

if (0) {
  console.log("This will never print");
}

🔢 6. parseInt() & parseFloat()

कभी-कभी string में number mixed होता है (जैसे "123px"),
तो Number() काम नहीं करेगा, तब हम parseInt() या parseFloat() use करते हैं।

console.log(parseInt("123px"));   // 123
console.log(parseFloat("12.34kg")); // 12.34

💡 7. Real-Life Example

👉 Suppose user ने form में number input किया लेकिन string में आया:

let price = "500";
let quantity = "3";

let total = Number(price) * Number(quantity);
console.log("Total Price:", total);


🟢 Output:

Total Price: 1500

🧾 8. Summary Table

| Conversion            | Function           | Example  | Result  |
| --------------------- | ------------------ | -------- | ------- |
| String → Number       | `Number("25")`     | `"25"`   | `25`    |
| Number → String       | `String(25)`       | `25`     | `"25"`  |
| Boolean → Number      | `Number(true)`     | `true`   | `1`     |
| Number → Boolean      | `Boolean(0)`       | `0`      | `false` |
| String → Boolean      | `Boolean("Hi")`    | `"Hi"`   | `true`  |
| Mixed String → Number | `parseInt("50px")` | `"50px"` | `50`    |

🧠 9. कब Use करें (When to Use)

| Situation                 | Conversion Needed      | Example                      |
| ------------------------- | ---------------------- | ---------------------------- |
| User input string में आया | String → Number        | `Number("10")`               |
| UI में show करना          | Number → String        | `String(100)`                |
| Condition check करना      | Value → Boolean        | `Boolean(value)`             |
| API से mixed data मिला    | Manual clean & convert | `parseInt()`, `parseFloat()` |

🚀 10. Practical Project Example
let userInput = prompt("Enter your age:");

if (Number(userInput) >= 18) {
  console.log("You are eligible to vote ✅");
} else {
  console.log("You are not eligible ❌");
}
